# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# CRITICAL RULE — NO EXCEPTIONS

Never make any code change without explicit user approval in that same turn. Always present the plan first and wait for a "yes" or "go ahead". This applies even when the fix is obvious. Violating this rule upsets the user.
